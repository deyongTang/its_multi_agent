#!/bin/bash
# GitHub Actions 自动部署脚本
# 用途：在远程服务器上自动拉取代码、构建镜像、启动服务

set -e  # 遇到错误立即退出

echo "=========================================="
echo "🚀 ITS Knowledge 知识库平台 - 自动部署"
echo "=========================================="

# ============================================
# 1. 环境检查
# ============================================
echo ""
echo "📋 步骤 1/7: 检查部署环境..."

# 检查 Docker
if ! command -v docker &> /dev/null; then
    echo "❌ 错误: Docker 未安装"
    exit 1
fi

# 检查 docker-compose
if ! command -v docker-compose &> /dev/null; then
    echo "❌ 错误: docker-compose 未安装"
    exit 1
fi

echo "✅ Docker 环境检查通过"

# ============================================
# 2. 准备项目目录
# ============================================
echo ""
echo "📂 步骤 2/7: 准备项目目录..."

PROJECT_DIR="/opt/its_multi_agent"

if [ -d "$PROJECT_DIR" ]; then
    echo "✅ 项目目录已存在: $PROJECT_DIR"
    cd "$PROJECT_DIR"

    # 检查是否为 Git 仓库
    if [ ! -d .git ]; then
        echo "⚠️  警告: 不是 Git 仓库，删除并重新克隆..."
        cd /opt
        rm -rf its_multi_agent
        git clone https://github.com/deyongTang/its_multi_agent.git
        cd its_multi_agent
    fi
else
    echo "📥 项目目录不存在，开始克隆仓库..."
    cd /opt
    git clone https://github.com/deyongTang/its_multi_agent.git
    cd its_multi_agent
fi

# ============================================
# 3. 拉取最新代码
# ============================================
echo ""
echo "📥 步骤 3/7: 拉取最新代码..."

# 配置 Git（避免认证问题）
git config pull.rebase false

# 拉取最新代码
git fetch origin main
git reset --hard origin/main

echo "✅ 代码更新完成"

# ============================================
# 4. 进入知识库目录
# ============================================
echo ""
echo "📂 步骤 4/7: 进入知识库目录..."

cd backend/knowledge

if [ ! -f docker-compose.yml ]; then
    echo "❌ 错误: docker-compose.yml 文件不存在"
    exit 1
fi

echo "✅ 当前目录: $(pwd)"

# ============================================
# 5. 停止旧容器
# ============================================
echo ""
echo "🛑 步骤 5/7: 停止旧容器..."

docker-compose down || true
echo "✅ 旧容器已停止"

# ============================================
# 6. 构建新镜像
# ============================================
echo ""
echo "🔨 步骤 6/7: 构建 Docker 镜像..."

docker-compose build --no-cache
echo "✅ 镜像构建完成"

# ============================================
# 7. 启动新容器
# ============================================
echo ""
echo "🚀 步骤 7/7: 启动新容器..."

docker-compose up -d
echo "✅ 容器启动完成"

# ============================================
# 8. 验证部署
# ============================================
echo ""
echo "🔍 验证部署状态..."
echo ""

# 等待服务启动
sleep 5

# 显示容器状态
echo "📊 容器状态:"
docker-compose ps

echo ""
echo "📝 容器日志（最后 20 行）:"
docker-compose logs --tail=20

echo ""
echo "=========================================="
echo "✅ 部署完成！"
echo "=========================================="
echo ""
echo "📝 API 文档: http://服务器IP:8001/docs"
echo "�� 查看日志: docker-compose logs -f"
echo "🔍 检查状态: docker-compose ps"
echo ""
